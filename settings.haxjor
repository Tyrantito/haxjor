<general>
##This is a block comment, but anything after the end of the block will continue reading.## authors = "Major" #currently no array support. btw, this comment isn't parsed either.
</general>

<config>
#Should debug messages appear? Quite a spam!
 debug_messages = true
#Should we run a script queue rather than denying them when ones running?
 script_queue = true
</config>